export const validGroupNameInputs = new RegExp(
  '^[a-zA-Z0-9]+( [a-zA-Z0-9]+)*$'
)

export const validDescriptionInputs = new RegExp(
  '^[a-zA-Z0-9]+( [a-zA-Z0-9]+)*$'
)

export const validIntegerInputs = new RegExp(
  '^(0|[0-9][0-9]*)$'
)